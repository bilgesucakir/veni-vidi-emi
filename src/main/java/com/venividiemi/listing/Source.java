package com.venividiemi.listing;

/**
 * A secondhand marketplace we aggregate listings from.
 * Facebook Marketplace is intentionally absent for now — no scrapeable surface.
 */
public enum Source {
    SAHIBINDEN,
    DOLAP
}
